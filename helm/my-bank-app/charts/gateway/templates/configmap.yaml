{{- if .Values.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "gateway.configMapName" . }}
  labels:
    {{- include "gateway.labels" . | nindent 4 }}
data:
  application-k8s.yml: |
    server:
      port: {{ .Values.service.port }}
    spring:
      application:
        name: gateway
      cloud:
        gateway:
          routes:
            {{- range .Values.routes }}
            - id: {{ .id }}
              uri: http://{{ $.Release.Name }}-{{ .service }}:{{ .port }}
              predicates:
                - Path={{ .path }}
            {{- end }}
      security:
        oauth2:
          resourceserver:
            jwt:
              issuer-uri: ${KEYCLOAK_ISSUER_URI:{{ .Values.keycloak.issuerUri }}}
    app:
      security:
        enabled: true
{{- end }}
