{{- if .Values.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgres.secretName" . }}
  labels:
    {{- include "postgres.labels" . | nindent 4 }}
type: Opaque
stringData:
  POSTGRES_DB: {{ .Values.database | quote }}
  POSTGRES_USER: {{ .Values.username | quote }}
  POSTGRES_PASSWORD: {{ .Values.password | quote }}
{{- end }}
