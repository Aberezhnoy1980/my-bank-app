# Prometheus (sprint 12)

Конфигурация Prometheus: `config/prometheus.yml`, развёртывание через Helm из `helm/my-bank-app/`.

Scrape targets для Spring Boot Actuator добавляются в коммите интеграции приложений.
