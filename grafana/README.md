# Grafana (sprint 12)

Дашборды и алерты — в `config/` (добавляются в коммите provisioning).

Provisioning: Prometheus datasource (`/prometheus` API prefix), dashboards (HTTP, JVM, business), Grafana alerting rules.

UI: http://localhost/grafana (admin / admin). Folder **MyBank** in Grafana.
